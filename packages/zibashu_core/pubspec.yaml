name: zibashu_core
description: Shared config, HTTP, and catalog models for ziBashu Android apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.7.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
